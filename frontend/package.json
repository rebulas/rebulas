{
  "name": "rebulas",
  "version": "0.0.1",
  "description": "Rebulas - Productivity for the technically minded",
  "scripts": {
    "watch": "export NODE_PATH=./js; watchify main.js -o bundle.js -dv",
    "server": "node server.js",
    "start": "npm-run-all -p -r watch server",
    "build": "export NODE_PATH=./js; browserify -d main.js > bundle.js",
    "test": "nodeunit"
  },
  "devDependencies": {
    "dropbox": "^2.5.4",
    "filereader": "^0.10.3",
    "node-localstorage": "^1.3.0",
    "nodeunit": "^0.11.1"
  },
  "dependencies": {
    "browserify": "^5.10.0",
    "dropbox": "^2.5.4",
    "elasticlunr": "^0.9.5",
    "localforage": "^1.5.0",
    "markdown-it": "^8.3.1",
    "marked": "^0.3.6",
    "npm-run-all": "^4.0.2"
  }
}
